import { describe,expect,it } from 'vitest';import { evaluatePolicy,type PolicyInput } from '../src/policy/engine.js';const base:PolicyInput={classification:{category:'PRIMARY',confidence:.99,replyRequired:true,sensitive:false,financial:false,legal:false,security:false,summary:'s',rationale:'r'},injectionScore:0,sender:'person@example.com',hasAttachments:false,autoSendEnabled:true,globalKillSwitch:false,minConfidence:.95,allowedCategories:['PRIMARY'],blockedSenders:[],maxDailySends:10,sendsToday:0,critiqueSafe:true,critiqueScore:.95};describe('policy engine',()=>{it('allows only fully safe inputs',()=>expect(evaluatePolicy(base).outcome).toBe('AUTO_SEND'));it('routes uncertain mail to review',()=>expect(evaluatePolicy({...base,classification:{...base.classification,confidence:.5}}).outcome).toBe('HUMAN_REVIEW'));it('blocks critical injection',()=>expect(evaluatePolicy({...base,injectionScore:.9}).outcome).toBe('BLOCK'));it('honors kill switch',()=>expect(evaluatePolicy({...base,globalKillSwitch:true}).outcome).toBe('HUMAN_REVIEW'));it('blocks no-reply mail',()=>expect(evaluatePolicy({...base,classification:{...base.classification,replyRequired:false}}).outcome).toBe('BLOCK'));});

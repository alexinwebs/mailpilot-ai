export interface InjectionAssessment { score:number; signals:string[]; risk:'LOW'|'MEDIUM'|'HIGH'|'CRITICAL' }
const patterns:[RegExp,string,number][]=[[/ignore (all |the )?(previous|system) instructions?/i,'instruction override',0.45],[/(reveal|show|print).*(system prompt|secret|api key|password)/i,'secret extraction',0.6],[/(send|forward|email).*(secret|credentials|private|another address)/i,'exfiltration request',0.55],[/(disable|bypass).*(security|policy|safety|review)/i,'security bypass',0.55],[/<\/?(system|assistant|tool|function)[^>]*>/i,'role markup',0.4],[/(do not tell|hide this from).*(user|owner)/i,'concealment',0.4],[/BEGIN (SYSTEM|DEVELOPER) (PROMPT|MESSAGE)/i,'prompt boundary forgery',0.5]];
export function detectPromptInjection(content:string):InjectionAssessment { const signals:string[]=[];let complement=1; for(const [pattern,label,weight] of patterns) if(pattern.test(content)){signals.push(label);complement*=1-weight;} const score=Math.min(1,1-complement); const risk=score>=0.8?'CRITICAL':score>=0.5?'HIGH':score>=0.25?'MEDIUM':'LOW';return {score,signals,risk}; }
